# j=1 long-smooth flank: sharp factorability / two-product DLS barrier — 2026-09-19

## Status
- Riemann hypothesis: OPEN.
- Context: (j=1), (1<alphale2), with
[
dasymp V,qquad
aasymp V^alpha,qquad
pasymp V^{3-alpha},
]
where (a) is squarefree and (V)-smooth.
- New result: preserving the prime factorization of (a) improves the direct DLS imbalance to a **uniform (V^{1/6}) loss**.
- This (1/6) loss is sharp for all methods that merely partition the original atomic factors into two product groups and then apply the standard DLS.
- Classification: `LONG_SMOOTH_TWO_PRODUCT_DLS_V16_BARRIER`.

## 1. Divisor selection formulation

Write
[
a=prod_i q_i,
qquad
lambda_i:=log_V q_i.
]

Since (a) is (V)-smooth and squarefree,
[
0<lambda_ile1,
qquad
sum_ilambda_i=alphain(1,2].
]

For a divisor
[
b=prod_{iin I}q_i,
]
put
[
t:=log_V b=sum_{iin I}lambda_i.
]

Use the product partition
[
x=db,
qquad
y=p(a/b).
]

Then
[
xasymp V^{1+t},
]
while
[
yasymp
V^{(3-alpha)+(alpha-t)}
=
V^{3-t}.
]

Thus the two exponents are symmetric around (2), with imbalance
[
oxed{delta=|t-1|}.
]

The standard product DLS therefore gives
[
|T|
ll_arepsilon
V^{3+arepsilon}V^{|t-1|/2}.
]

## 2. Universal (1/3)-approximation lemma

For every finite collection
[
0<lambda_ile1,
qquad
sum_ilambda_i>1,
]
there exists a subset (I) such that
[
oxed{
rac23le
sum_{iin I}lambda_i
le
rac43.
}
]

Proof:
- if some (lambda_iin[2/3,1]), choose that single item;
- otherwise every (lambda_i<2/3). Add items until the partial sum first reaches (2/3). The previous sum was (<2/3), and the added item is (<2/3), so the new sum is (<4/3).

Hence one can always choose (bmid a) with
[
|t-1|lerac13.
]

Therefore
[
oxed{
T_{j=1, alpha>1}
ll_arepsilon
V^{3+1/6+arepsilon}
}
]
by factorization-preserving two-product DLS.

This improves the atomic partition deficit
[
V^{(alpha-1)/2}
]
uniformly.

## 3. Sharpness of the (1/3) exponent discrepancy

Take
[
alpha=rac43
]
and suppose
[
a=q_1q_2,
qquad
q_1asymp q_2asymp V^{2/3}.
]

Then
[
pasymp V^{3-alpha}=V^{5/3}.
]

If the outer Möbius variable (dasymp V) is atomic, the original factor exponents are
[
oxed{
1,quad
rac53,quad
rac23,quad
rac23.
}
]

Their total is (4).

All subset exponents are generated by these four numbers. The values nearest to (2) are
[
rac53
quad	ext{and}quad
rac73,
]
both at distance
[
rac13.
]

There is no subset product of scale
[
V^{2+o(1)}
]
closer than this.

Therefore any strategy of the form
[
	ext{partition original atomic factors into two groups}
	o
	ext{standard DLS}
]
must, in this admissible configuration, retain at least the loss
[
oxed{V^{1/6}}.
]

Thus the universal (V^{1/6}) bound is sharp within the two-product partition class.

## 4. Relation to simpler atomic partitions

Without splitting (a), two obvious partitions give discrepancies
[
delta_1=alpha-1
quad (damid p),
]
and
[
delta_2=2-alpha
quad (dpmid a).
]

Taking the better of them gives
[
min(alpha-1,2-alpha),
]
whose worst value is (1/2) at (alpha=3/2), corresponding to a DLS loss (V^{1/4}).

Thus preserving the internal factorization of (a) improves the worst two-product loss
[
V^{1/4}
	o
oxed{V^{1/6}}.
]

## 5. What remains

The sharp configuration is essentially
[
dasymp V,quad
pasymp V^{5/3},quad
q_1,q_2asymp V^{2/3}.
]

A generic two-product large sieve cannot close it.

Possible next inputs are:
1. use factorability of the outer Möbius variable (d) as well; this helps except when (d) is itself atomic/prime-scale;
2. use a genuinely three- or four-dimensional spacing inequality rather than collapsing to two products;
3. exploit the prime/Möbius nature of the four coefficients;
4. split the atomic (d)-sector by its own large-prime structure and treat the prime-(d) extremal case separately.

Permanent rules:
- `SMOOTH_FACTORABILITY_BEFORE_ATOMIC_DLS`.
- `TWO_PRODUCT_PARTITION_V16_SHARP_GUARD`.

# j=2 fixed-range semiprime centered-variance / dispersion audit — 2026-09-19

## Status
- Riemann hypothesis: OPEN.
- Sector: (j=2), worst asymmetric endpoint (alpha=0).
- Common balance loss: (V^{1/4}).
- Common residual sufficient condition: diagonal-scale centered variance for the long-side (E_2)-type coefficient.
- New localization: the natural residual is a **different-small-factor bilinear dispersion** problem; a same-factor Cauchy treatment discards the needed information.
- Classification: `J2_DIFFERENT_FACTOR_DISPERSION_OPEN`.

## 1. Sharp scales

At (alpha=0),
[
a=1,qquad dasymp V,qquad p,qasymp V^{3/2}.
]

Use the optimal two-product split
[
x=qasymp V^{3/2},
qquad
y=dpasymp V^{5/2}.
]

Write the long-side scale as
[
S:=V^{5/2}.
]

Then
[
D:=V=S^{2/5},
qquad
P:=V^{3/2}=S^{3/5}.
]

The normalized reciprocal parameter is
[
X=V^2,
]
so the arithmetic long-side DLS cell length is
[
H=S/X=V^{1/2}=S^{1/5}.
]

## 2. Centered coefficient

Let
[
a_n=
sum_{substack{n=dp\dasymp D\pasymp P {m prime}}}alpha_d,
]
where (alpha_d) is the inherited Möbius/divisor-bounded coefficient.

Let (ho(t)) denote the smooth local mean density of this product sequence.

A sufficient residual estimate is
[
J_a(S,H)
=
int_S^{2S}
left|
sum_{x<nle x+H}a_n
-
int_x^{x+H}ho(t),dt
ight|^2dx
ll_arepsilon
HS^{1+arepsilon}.
]

At
[
H=S^{1/5}
]
this is
[
oxed{
J_a(S,S^{1/5})
ll_arepsilon
S^{6/5+arepsilon}.
}
]

## 3. Correlation expansion

After the usual Selberg-integral identity / smoothing, the off-diagonal part is controlled by shifts
[
|h|lesssim H
]
and correlations of the form
[
sum_{d,d'asymp D}
alpha_doverline{alpha_{d'}}
sum_{substack{p,p'asymp P\dp-d'p'=h}}1.
]

This is the exact arithmetic residual hidden by the unsigned DLS cell count.

## 4. Why same-(d) Cauchy is not the right reduction

In several almost-prime short-interval arguments one first Cauchy-squares over a small prime/factor (d), retaining correlations with the same (d).

Here
[
D=S^{2/5}>H=S^{1/5}.
]

For the same (d),
[
d(p-p')=h,
qquad |h|<D,
]
forces
[
h=0,qquad p=p'
]
except for endpoints/zero shift.

Thus the same-factor off-diagonal disappears.

But Cauchy has already paid for the number of (d)-blocks. The remaining diagonal contribution contains precisely the polynomial factor that the common residual must recover.

Hence
[
oxed{
	ext{same-factor Cauchy removes the wrong off-diagonal and loses the needed power.}
}
]

Permanent guard:
`SAME_FACTOR_CAUCHY_POWER_LOSS`.

## 5. Preserve distinct (d,d')

Keep (d
e d') before Cauchy.

Write
[
g=(d,d'),qquad d=ga,qquad d'=gb,qquad (a,b)=1.
]

The equation
[
dp-d'p'=h
]
requires
[
gmid h.
]

Put
[
h=gk.
]

Then
[
ap-bp'=k.
]

Modulo (b),
[
apequiv kpmod b.
]

Since ((a,b)=1),
[
oxed{
pequiv kar apmod b.
}
]

Similarly
[
p'equiv -kar bpmod a.
]

Thus the centered variance is naturally a bilinear dispersion problem for primes in residue classes whose moduli are generated by the *other* small factor.

## 6. Natural modulus scales

For generic small gcd (g),
[
a,basymp D=S^{2/5}.
]

The prime length is
[
P=S^{3/5}.
]

Hence the modulus relative to the prime length is
[
D=P^{2/3}.
]

This is beyond the classical Bombieri--Vinogradov square-root range
[
P^{1/2}.
]

Therefore a direct black-box application of Bombieri--Vinogradov does not control the generic (g=O(1)) portion.

However the problem contains additional averaging over
[
d,d',h,
]
and the residue
[
kar apmod b
]
has reciprocal/Kloosterman structure. This is precisely the geometry in which dispersion and averaged Kloosterman-fraction estimates can outperform individual prime-progressions bounds.

## 7. Literature position

Existing exact-(E_2) and almost-prime short-interval results demonstrate strong Type-II/dispersion technology, but they do not give the above fixed-range centered variance as an immediate black box.

In particular:
- existence of (E_2) numbers in almost all much shorter intervals does not imply the required centered (L^2) asymptotic for this exact (S^{2/5}	imes S^{3/5}) coefficient;
- prime--(E_2) averaged correlations down to shift ranges around (X^{1/6+arepsilon}) do not automatically imply this (E_2)-type self-variance;
- the special-(E_2) minor-arc argument based on same-small-factor Cauchy loses the relevant power in the present (D>H) geometry.

## 8. Exact next analytic target

A sufficient theorem would control the centered dispersion
[
mathcal D
=
sum_{|h|lesssim H}W(h/H)
sum_{d,d'asymp D}
alpha_doverline{alpha_{d'}}
left[
sum_{substack{p,p'asymp P\dp-d'p'=h}}1
-
mathfrak M(d,d',h)
ight]
]
by
[
oxed{
mathcal D
ll_arepsilon
HS^{1+arepsilon}.
}
]

Here (mathfrak M) is the local density/singular main term consistent with the smooth cell mean already separated in the common reduction.

This is the current (j=2) residual theorem target.

## 9. Interpretation

The (j=2) problem is no longer “prove a semiprime variance theorem” in an undifferentiated sense.

It is:
[
oxed{
	ext{preserve different small factors until the dispersion/Kloosterman stage.}
}
]

If a Kloosterman/dispersion estimate supplies the required extra
[
V^{-1/4}
]
relative to ordinary two-product DLS, the (j=2) sector closes at
[
V^{3+arepsilon}.
]

If current unconditional dispersion technology provably falls short, record the exact exponent gap and move to the next sector without conflating existence theorems with the needed centered variance.

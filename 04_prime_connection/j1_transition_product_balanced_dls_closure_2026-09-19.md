# j=1 reciprocal transition closure by product-balanced double large sieve — 2026-09-19

## Status
- Riemann hypothesis: OPEN.
- Module: bounded-large-prime sector (j=1) at the critical Vaughan edge.
- Previous pointwise target (sum_{pasymp P} e(Cp^{-1/2})ll P^{1/2+arepsilon}): **NOT NECESSARY** for the original bilinear module.
- New result: the exact transition block (aasymp V, pasymp V^2) is bounded at the required scale by a product collapse (r=da), Mellin separation of the product cutoff, and the Bombieri--Iwaniec double large sieve.
- Classification: `J1_TRANSITION_DLS_CLOSED`.

## 1. Critical edge scales

Let
[
V=K^{1/4}.
]
At the hardest (j=1) transition,
[
dasymp V,qquad aasymp V,qquad pasymp V^2,
]
and the reciprocal phase inherited from the (Q=O(1)) edge has the form
[
e!left(A(dap)^{-1/2}ight),
qquad Aasymp V^4.
]

The required total bound for this block is
[
oxed{V^{3+arepsilon}}.
]

## 2. Why the earlier pointwise prime target was too strong

If (d,a) are frozen, then
[
C_{d,a}=rac{A}{sqrt{da}}asymp V^3
]
and the inner prime sum is
[
S_{d,a}=sum_{pasymp V^2} b_p e(C_{d,a}p^{-1/2}).
]

Demanding
[
|S_{d,a}|ll V^{1+arepsilon}
]
uniformly in every (d,a) is sufficient, but it discards the fact that the frequencies (C_{d,a}) are generated by the outer arithmetic variables.

The original module should therefore be estimated before taking a pointwise supremum in (C).

Permanent correction:
`POINTWISE_PRIME_TRANSITION_TARGET_TOO_STRONG`.

## 3. Product collapse of the two short arithmetic variables

Set
[
r=da.
]
Then
[
rasymp V^2.
]

For (j=1), squarefreeness and (p>Vge P^+(a)) give
[
mu(ap)=-mu(a).
]

After collapsing (d,a) to (r), the arithmetic coefficient has the form
[
A_r(t)
=
sum_{substack{da=r\dasymp V, aasymp V\P^+(a)le V}}
mu(d)mu(a),W_d(d/V)W_a(a/V)a^{it},
]
after Mellin separation of any smooth product cutoff in (ap/V^3).

Uniformly in real (t),
[
|A_r(t)|le 	au(r),
]
and hence the standard divisor second moment gives
[
sum_{rasymp V^2}|A_r(t)|^2
ll
V^2(log V)^{O(1)}.
]

No cancellation in (A_r) is used.

## 4. Mellin separation of the product cutoff

A smooth cutoff of the form
[
W(ap/V^3)
]
can be written by Mellin inversion as
[
W(ap/V^3)
=
rac{1}{2pi}
int_{mathbb R}
widetilde W(t)
(a/V)^{it}(p/V^2)^{it},dt,
]
with rapidly decaying (widetilde W(t)).

Thus it is enough, uniformly in (t), to bound
[
T_t
=
sum_{rasymp V^2} A_r(t)
sum_{pasymp V^2} b_p(t)
e!left(A(rp)^{-1/2}ight),
]
where
[
|b_p(t)|le 1
]
for the prime-indicator formulation.

The (t)-integration adds only a harmless constant/polylogarithmic factor.

## 5. Normalized double-large-sieve form

Put
[
X:=rac{A}{V^2}asymp V^2,
]
[
u(r):=left(rac{r}{V^2}ight)^{-1/2},
qquad
v(p):=left(rac{p}{V^2}ight)^{-1/2}.
]

Then
[
A(rp)^{-1/2}
=
X,u(r)v(p),
]
and (u,vasymp1).

The Bombieri--Iwaniec double large sieve bounds a bilinear form
[
sum_rsum_p A_r b_p e(Xu(r)v(p))
]
by the product of the (1/X)-spacing energies in the two normalized variable sets.

## 6. (r)-spacing count

For (rasymp V^2),
[
u'(r)asymp V^{-2}.
]

Since
[
X^{-1}asymp V^{-2},
]
the condition
[
|u(r)-u(r')|lesssim X^{-1}
]
forces
[
|r-r'|ll1.
]

Therefore the weighted spacing energy satisfies
[
B_r
:=
sum_{substack{r,r'\|u(r)-u(r')|lesssim X^{-1}}}
|A_rA_{r'}|
ll
sum_{rasymp V^2}|A_r|^2
ll
V^2(log V)^{O(1)}.
]

## 7. Prime spacing count

For (pasymp V^2),
[
v'(p)asymp V^{-2}.
]

Again
[
|v(p)-v(q)|lesssim X^{-1}
]
implies
[
|p-q|ll1.
]

Hence each prime has only (O(1)) integer candidates (q) in the spacing cell, and
[
B_p
:=
sum_{substack{p,q\|v(p)-v(q)|lesssim X^{-1}}}
|b_pb_q|
ll
V^2
]
trivially. Using prime density would improve only logarithms and is unnecessary.

## 8. Closure

The one-dimensional Bombieri--Iwaniec double large sieve gives schematically
[
|T_t|^2
ll
X,B_rB_p.
]

Substituting
[
Xasymp V^2,
quad
B_rll V^2(log V)^{O(1)},
quad
B_pll V^2,
]
yields
[
|T_t|^2
ll
V^6(log V)^{O(1)}.
]

Therefore
[
oxed{
|T_t|
ll_arepsilon
V^{3+arepsilon}.
}
]

After Mellin inversion,
[
oxed{
T_{j=1, aasymp V}
ll_arepsilon
V^{3+arepsilon}.
}
]

This is exactly the required edge scale.

## 9. General (j=1) scale audit

More generally let
[
aasymp V^alpha,
qquad
pasymp V^{3-alpha},
qquad
0lealphale2.
]

Then
[
r=daasymp V^{1+alpha},
qquad
rpasymp V^4,
qquad
Xasymp V^2.
]

The normalized spacing multiplicities are
[
1+rac{V^{1+alpha}}{V^2}
=
1+V^{alpha-1}
]
on the (r)-side and
[
1+rac{V^{3-alpha}}{V^2}
=
1+V^{1-alpha}
]
on the (p)-side.

Therefore the same DLS argument gives the scale
[
oxed{
|T_alpha|
ll_arepsilon
V^{3+arepsilon}
Bigl[(1+V^{alpha-1})(1+V^{1-alpha})Bigr]^{1/2}.
}
]

Hence:
- (alpha=1): exact target (V^{3+arepsilon}), CLOSED;
- (alpha<1): deficit (V^{(1-alpha)/2});
- (alpha>1): deficit (V^{(alpha-1)/2}).

Thus the transition is not the hardest surviving point after all: it is the unique self-dual scale at which the standard double large sieve closes exactly.

## 10. Consequence for the research map

The previous active item
`J1_PRIME_RECIPROCAL_TRANSITION_OPEN`
is closed and replaced by two asymmetric flanks:

1. `J1_LONG_PRIME_FLANK_OPEN`: (0lealpha<1), where the prime variable is longer than (V^2);
2. `J1_LONG_SMOOTH_MOBIUS_FLANK_OPEN`: (1<alphale2), where the collapsed (r=da) side is longer than (V^2).

The next audit should exploit the *direction* of the imbalance rather than force another symmetric DLS estimate.

## References
- Bombieri--Iwaniec double large sieve, in the form recalled as Lemma 8 of Robert--Sargos, *Three-dimensional exponential sums with monomials*, J. reine angew. Math. 591 (2006), 1--20.

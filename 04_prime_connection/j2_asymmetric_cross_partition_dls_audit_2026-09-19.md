# j=2 asymmetric sector: optimal cross-product DLS audit — 2026-09-19

## Status
- Riemann hypothesis: OPEN.
- Context: bounded-large-prime sector (j=2) at the Vaughan edge.
- Previous balanced endpoint (aasymp V): CLOSED.
- New result: among the elementary two-product partitions, crossing one short Möbius variable with the smaller large prime cuts the direct DLS imbalance exponent in half.
- This does not close the full asymmetric sector, but it sharpens the exact missing power.
- Classification: `J2_CROSS_PARTITION_DEFICIT_HALVING`.

## 1. Scale parameters

Write
[
dasymp V,qquad
aasymp V^alpha,qquad
pasymp V^eta,qquad
qasymp V^gamma,
]
with
[
0lealpha<1,qquad
1leetalegamma,qquad
alpha+eta+gamma=3.
]

The full multiplicative argument satisfies
[
dapqasymp V^4,
]
and the reciprocal oscillatory amplitude is (Aasymp V^4).

For any partition into product variables
[
xasymp V^ho,qquad yasymp V^{4-ho},
]
the normalized Bombieri--Iwaniec DLS frequency parameter is
[
Xasymp V^2.
]

The spacing multiplicities are
[
1+V^{ho-2},
qquad
1+V^{2-ho},
]
so the DLS loss beyond the target (V^{3+arepsilon}) is
[
oxed{V^{|ho-2|/2}}
]
up to (V^arepsilon).

## 2. Old partition: (damid pq)

The first group has exponent
[
ho_{da}=1+alpha.
]

Hence
[
|ho_{da}-2|=1-alpha,
]
and the old bound has deficit
[
oxed{V^{(1-alpha)/2}}.
]

## 3. Cross partition: (dpmid aq)

Take the smaller large prime (p), so (etalegamma).

Then
[
x=dpasymp V^{1+eta},
qquad
y=aqasymp V^{alpha+gamma}.
]

Using
[
alpha+eta+gamma=3,
]
we have
[
alpha+gamma=3-eta.
]

Thus the two product exponents are
[
1+eta,qquad 3-eta,
]
symmetric around (2).

Their imbalance is
[
oxed{delta=eta-1}.
]

The resulting DLS estimate is therefore
[
oxed{
|T_{j=2,alpha,eta}|
ll_arepsilon
V^{3+arepsilon}V^{(eta-1)/2}.
}
]

The product coefficients are divisor-bounded:
- on the (dp) side, a Möbius coefficient convolved with a prime indicator;
- on the (aq) side, a (V)-smooth squarefree Möbius coefficient convolved with a prime indicator.

After smooth cutoff separation their second moments are bounded by the corresponding product lengths times polylogarithms, so no cancellation assumption is used.

## 4. Worst case at fixed (alpha)

Since
[
etalegamma
]
and
[
eta+gamma=3-alpha,
]
we have
[
etalerac{3-alpha}{2}.
]

Therefore
[
eta-1
le
rac{1-alpha}{2}.
]

Hence uniformly over the (j=2) asymmetric sector,
[
oxed{
|T_{j=2,alpha}|
ll_arepsilon
V^{3+arepsilon}
V^{(1-alpha)/4}.
}
]

The worst case is the balanced-prime subflank
[
eta=gamma=rac{3-alpha}{2}.
]

Thus the optimal elementary cross-product partition cuts the previous deficit
[
V^{(1-alpha)/2}
]
to
[
oxed{V^{(1-alpha)/4}}.
]

## 5. Why this is not yet closure

For fixed (alpha<1), the remaining factor
[
V^{(1-alpha)/4}
]
is still polynomial.

Therefore divisor-bounded coefficient moments plus a single standard DLS application do not close the full (j=2) asymmetric sector.

The residual difficulty is concentrated where the two large primes are comparable:
[
pasymp qasymp V^{(3-alpha)/2}.
]

If one prime is closer to (V), then (eta-1) is smaller and the DLS loss improves automatically.

## 6. Optimality among elementary unsplit partitions

The relevant subset exponents are generated by
[
{1,alpha,eta,gamma}.
]

Because (etalegamma):
- (1+eta) is closer to (2) than (1+gamma);
- (alpha+gamma=3-eta) is its complement;
- (1+alpha) has distance (1-alpha);
- (alpha+eta=3-gamma) has distance (gamma-1geeta-1);
- one-variable partitions have distance at least as large in the admissible range.

Hence (dpmid aq) is the best elementary two-group partition when (p) is chosen as the smaller large prime.

## 7. New exact frontier

The (j=2) asymmetric problem is reduced to:

[
oxed{
alpha<1,quad
pasymp qasymp V^{(3-alpha)/2},
quad
	ext{missing saving }V^{(1-alpha)/4}.
}
]

The next possible gain cannot come from merely choosing a different unsplit product partition.

It must use one of:
1. factorability of one arithmetic coefficient before product collapse;
2. an iterated/multilinear large sieve that uses all four original variables;
3. cancellation specific to the Möbius/smooth coefficient;
4. a decomposition of the prime indicator that creates an additional balancing variable.

Permanent rule:
`OPTIMIZE_PRODUCT_PARTITION_BEFORE_MULTILINEAR_INPUT`.
